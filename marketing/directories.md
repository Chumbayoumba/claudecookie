# Directory & listing drafts (DRAFTS — not published yet)

## 1. public-apis (github.com/public-apis)

Entry for `Websites`/`General` section, in the repo's format:

```
claudecookie | Convert Netscape cookies.txt / Cookie-Editor / Puppeteer / key-value / Cookie header formats; check a Claude session (plan, 5h and weekly usage); mint ~/.claude/.credentials.json — public JSON API, no key, CORS *. https://claudecookie.com/api/
```

The full APILayer-style promotional block is in `../github-promo/tmp-public-apis.md` — reuse it for the public-apis PR body.

## 2. dev.to

Use `dev-to-post.md` (this directory). Post under tags: claude, claude-code, javascript, api, devtools.

## 3. Hacker News

Use `show-hn.md` for the Show HN. Also consider a plain item:
- Title: "claudecookie.com – convert, check, and mint Claude Code credentials from a cookie"
- URL: https://claudecookie.com/api/

## 4. Awesome lists

- `awesome-claude-code` (github.com/ebardoni/awesome-claude-code) — add under "Tools": `claudecookie – cookie conversion, session check, and .credentials.json minting, plus a public no-key JSON API (https://claudecookie.com/api/)`
- `awesome-api` / `public-apis` as above.

## 5. Product Hunt (optional)

- Tagline: "Paste a Claude cookie, check the session, and download the credentials file Claude Code reads — website + no-key JSON API."
- Topics: Developer Tools, Web App, Productivity
